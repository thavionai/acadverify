import { CredentialRegistryTable } from "@/components/dashboard/credential-registry-table";

export const metadata = { title: "Registry | AcadVerify" };

export default function RegistryPage() {
  return <CredentialRegistryTable />;
}

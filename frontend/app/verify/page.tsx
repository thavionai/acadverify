import { VerifyLookup } from "@/components/verify/verify-lookup";

export const metadata = {
  title: "Verify Credential | AcadVerify",
};

export default function VerifyPage() {
  return <VerifyLookup />;
}
